ls										//liste tout les fichier d'un repertoire
ls -a									//Liste tout les fichier même ceux cachés
rm -rf .git 							//Supprime le répertoire git
echo hello > file1.txt 					// Crée le fichier file1.txt content "hello"
mv file1.txt main.js					//renome file1.txt en main.js